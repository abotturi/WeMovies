import { IPropsChildren } from "../../../interfaces/IPropsChildren";

export interface IPropMainLayout extends IPropsChildren {
    loading?: boolean,
    resultSize?: number,
    showSearch?: boolean
}